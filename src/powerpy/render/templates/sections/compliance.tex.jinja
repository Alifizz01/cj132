\section{Compliance overview}
<<% if has_compliance %>>
\begin{figure}[H]
  \centering
  \includegraphics[width=0.92\linewidth]{<< compliance_figure >>}
  \caption{Power at the bus voltage, per case, against the requirement.}
\end{figure}

\begin{tabular}{lrrr}
  \toprule
  Case & P at \(V_{bus}\) [W] & Requirement [W] & Margin [\%]\\
  \midrule
<<% for c in cases %>>
<<% if c.bus %>>
  \texttt{<< c.case.label | tex >>} & << c.bus.power_w | num(".1f") >>
                                    & << requirement_w | num(".0f") >>
                                    & << (100.0 * (c.bus.power_w / requirement_w - 1.0)) | num("+.2f") >>\\
<<% endif %>>
<<% endfor %>>
  \bottomrule
\end{tabular}
<<% else %>>
\textit{No bus voltage or requirement set; compliance check skipped.}
<<% endif %>>
